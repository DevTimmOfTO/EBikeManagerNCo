enum BatteryEventType { chargeCycle, healthCheck, batteryReplaced }

enum TripSource { manual, healthConnect, inAppGpsRecording }

enum ThemePreference { system, light, dark }

enum UnitSystem { metric, imperial }
